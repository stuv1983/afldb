<!-- DOC: Step session summary for the AFLDB-ISSUE-224 planning run -->
<!-- DOC-SCOPE: PhanesLight working memory; AFLDB issues.md / IssuesIndex.md remain authoritative -->

# SS00004 — AFLDB-ISSUE-224 planning run (2026-09-19)

**Agent:** `afldb-orchestrator` (Opus 5, high effort). **Tier:** T3 (multi-module: import toolchain,
canonical identity/registration, admin mutation surface, database schema).
**Worktree:** `D:\dev\afldb-issue-224`. **Branch:** `sonnet/issue-224`. **HEAD:** `77456b04`
(working tree clean at start).

**Task:** plan AFLDB-ISSUE-224 — the 94 DraftGuru persons withheld `target_not_registered` by the
ISSUE-222 person bridge. **Planning only.** No implementation, no database, no network, no commit.

**Outcome: plan review returned REVISE on three HIGH findings; the run is stopped and referred to the
operator per CLAUDE.md §15.** This summary covers **both** planning passes — pass 1 (interrupted
before its mandatory review) and pass 2 (verification, correction and review). It is kept as one
summary because it is one task resumed, and because the generated `_index.md` may not be hand-edited
and `doc-index` could not be run under §9.

## Boundary held

No shell/Git/SQL/SSH/deployment/test/import/migration command was executed against any target by this
agent or any agent it spawned. No database was contacted (production, `afldb_dev`, `afldb_test`). No
player, shell, identity, link, decision, override or import batch was created. Nothing was committed,
pushed, merged or deployed. CLAUDE.md §9/§12 were restated verbatim in every spawn prompt.

Work performed was: repository file reads, code search (`semble` + Grep), read-only `git status` /
`git check-ignore`, and offline arithmetic over tracked JSON/CSV artefacts.

## Agents spawned (4)

| Agent | Purpose | Return persisted at |
|---|---|---|
| `afldb-worker` | Registration authority: `external_identities` schema, every writer, `import_fitzroy_core.py`, the settle path, admin paths, player-shell guards | `.phaneslight/returns/issue-224-plan/R01-worker-registration-authority.md` |
| `afldb-worker` | DraftGuru bridge export/import contract: `--resolve-against`, gate, `apply_authority()` HALTs, transaction boundary, idempotence, offline-vs-DB table | `.phaneslight/returns/issue-224-plan/R02-worker-bridge-import-contract.md` |
| `afldb-mechanic` | Digest of `docs/acquisition/AFLDB-2026-API-ACQUISITION.md` (1,126 lines) | `.phaneslight/returns/issue-224-plan/R03-mechanic-2026-acquisition-digest.md` |
| `afldb-reviewer` | Mandatory plan review of the completed runbook (CLAUDE.md §15) | see §"Review" below |

`afldb-closure` was deliberately **not** spawned: planning-only, no code or data change to close out.
Peak concurrency 2, well inside the 5-agent bound. No agent was forked; every spawn carried a
self-contained brief.

**Note:** `.phaneslight/returns/` is gitignored (`.gitignore:40`), by design. The returns are durable
on disk but will not be committed.

## Outcome

Root cause established as an **architectural boundary, not a defect**: the canonical player register
is written only by `tools/migration/import_fitzroy_core.py` from an accepted **1897–2025** snapshot,
and that importer is "Never applied in-season" (docstring `:52`); the in-season settle path
*structurally cannot* register an identity (`src/lib/acquisition/settle-afltables.ts:2664-2673`, its
own §17.3 rule, zero `INSERT INTO players` / `INSERT INTO external_identities` in the file); the
end-of-season rollover that would extend the baseline
(`docs/acquisition/AFLDB-2026-API-ACQUISITION.md:388`) was never opened or built.

Population bounded at **exactly 94, identical on `afldb_test` and `afldb_dev`**, reconciling
(1,493 + 94 + 2 = 1,589 withheld; 3,468 + 94 = 3,562 parent). **No tracked artefact distinguishes one
of the 94 from another** — all carry `insufficient_evidence`. Classification therefore requires new
evidence, which is the plan's Phase 1.

Pivotal verified fact: the super_admin `attachAflTablesIdentity`
(`src/db/queries/admin-draft.ts:1492-1502`) writes exactly the tuple `export_person_bridge.py`'s
`REGISTRATION_SQL` (`:351-358`) measures, so the reviewed per-person registration route is real. No
bulk registration path exists anywhere in the repository.

Deliverable: `issues/open/AFLDB-ISSUE-224.md` — 12 sections, 9 phases with HALTs, idempotence proofs
and rollback, a 7-protection safety matrix, 7 rejected alternatives, 6 operator decisions.

## Findings and dispositions

| ID | Grade | Disposition |
|---|---|---|
| F-224-3 | **MED** | Dean Laidley (99 DraftGuru games, b.1967, no draft event) and Matthew Capuano (107 games, 1992) cannot be 2026 debutants; Laidley appears nowhere in the ISSUE-222 record. **Handled by the orchestrator** (MED never reaches the reviewer): Category B isolated, HALTs H1.2/H1.3 written into Phase 1, operator decision D-2 raised. |
| F-001 (mechanic) | MED → INFO | ISSUE-092 population-drop gate undefined in the acquisition doc. **Resolved**: definition is `tools/migration/common.py:129-159`, wired into `import_fitzroy_core.py:2804-2811`. Creates no work. |
| F-224-1/2/4/5 | INFO | Recorded in the runbook §11. No work created. |

**No HIGH or CRIT was raised by the orchestrator.** No finding was deferred.

## Disclosed drift

Three **zero-byte** untracked files appeared at the worktree root during this session (16:45–16:57),
created by an unquoted shell redirection inside a subagent's search command:
`afldb_normalise_name(display_name)`, `normalised`, `{'`. They are this session's own drift,
disclosed rather than hidden, and deliberately **not deleted** — file removal is a shell mutation and
no such authorisation was granted (§9). Operator action: delete all three.

## Files changed

Created: `issues/open/AFLDB-ISSUE-224.md`; three return files under
`.phaneslight/returns/issue-224-plan/`; this summary.
Modified: `issues.md` (ISSUE-224 planning outcome appended), `IssuesIndex.md` (state, runbook pointer,
next action).
**No source, test, tool, migration, manifest or reference-data file was touched.**

## Pass 2 (same day, after the operator interrupted pass 1 before its plan review)

Pass 1's runbook was retained but **not trusted**. Pass 2 re-verified every load-bearing claim against
current source, then ran the mandatory review. Agents: two `afldb-worker` verifications and one
`afldb-reviewer` plan review; returns persisted under `.phaneslight/returns/issue-224-plan-v2/`
(gitignored, `.gitignore:40`). Peak concurrency 2. `afldb-closure` deliberately not spawned
(planning-only). Boundary held exactly as in pass 1: no database, no network, no import, no migration,
no mutation, no commit; read-only `git status`/`diff` only.

### Plan review — VERDICT: **REVISE**

Three HIGH findings, six MED, two LOW, one INFO. **All three HIGHs independently re-verified by the
orchestrator against source and accepted.** Per CLAUDE.md §15 a HIGH in plan review **stops the run
and goes to the operator** — it has, and nothing is approved for execution.

| ID | Grade | What it overturned |
|---|---|---|
| F-R01 | HIGH | Category B (Laidley, Capuano) is **already explained** by the AFLDB-ISSUE-222 entry: both are *already-registered* players (3208 → `Dani_Laidley.html`; 9198 → `Mathew_Capuano.html`) whose captured href differs from the registered path. Pass 1's "Laidley appears nowhere in the ISSUE-222 record" was **false** and had been propagated into `issues.md`. Orchestrator additionally measured **101 − 94 = 7**, reconciling exactly to the cases the v2 parent correction already fixed — which identifies the proven remedy |
| F-R02 | HIGH | A **fourth** AFL Tables registering writer, `enrich_birth_dates.py:565-577` — bulk writer *and* bulk deleter. "No bulk registration path exists anywhere" withdrawn |
| F-R03 | HIGH | **Two hash-pinned 2026 `in_season_partial` snapshots already exist** (`issue129-t7-20260903.json`, 9,614 rows, `Career.Games` present). D-1's "the 94 cannot be classified at all without a new acquisition" was false |

MEDs F-R04–F-R09 and LOWs F-R11 accepted and corrected. **F-R10 proved an orchestrator finding
(F-224-15) wrong** — `full_history.current_season_excluded` does exist; retracted in place.

### Orchestrator findings in pass 2 (before the review)

F-224-6 (MED, Phase 1 command unexecutable), F-224-7 (MED, 668 not 663 distinct URLs; extract on
`url` not `ID`), F-224-9 (discriminator rebuilt on AFL Tables' own `Career.Games`), F-224-10/11 (HIGH,
from worker verification: the replay registering path and `seed_player`), F-224-12/13/14/17. F-224-13
is notable as a case where a worker's code claim was **accepted** and its consequence **rejected** on
the orchestrator's own measurement.

### F-224-18 (MED, self-assessed methodology finding)

The registering-writer enumeration was wrong **three times** (2 → 3 → 4), each time asserted as
complete. Two causes, both recorded in the runbook §11.1: a Grep fallback enumeration (after `semble`
was blocked by the permission classifier) was treated as exhaustive rather than provisional; and **the
AFLDB-ISSUE-222 ledger entry was never read**, although CLAUDE.md §5 makes it authoritative and
prescribes exactly that narrow targeted read. Measurable artefacts were over-weighted because they
were measurable; the authoritative prose record was under-read.

## Disclosed drift — now SEVEN files, not three

Pass 2's own verification greps added four more zero-byte artefacts of unquoted shell redirection:
`[(player_id`, `canonical`, `draft_picks`, `registered-row`, joining pass 1's
`afldb_normalise_name(display_name)`, `normalised`, `{'`. All zero-byte, all untracked, none deleted
(file removal is a shell mutation, §9). **Operator action: delete all seven.**

## Next

**Operator decisions, in order.** **D-1:** are the `issue129-t7-20260903` snapshot *bytes* still on
disk in the main checkout (gitignored path, not in this worktree)? If yes, classifying Category A's 92
on `Career.Games` is **fully offline — no network, no database**; if no, authorise a fresh in-season
acquisition. **D-1a:** finals-inclusive acquisition wanted anyway (that snapshot predates the 2026
finals)? **D-2:** D-9 adjudication of the two corrected identities for Laidley and Capuano — a
parent-evidence correction, **not** a registration. **D-4:** open the end-of-season rollover as its own
issue; the review showed its blast radius far exceeds ISSUE-224, which should consume its result.
