<!-- DOC | Pre-merge hardening pass correcting the issue register and compacting CLAUDE.md -->
<!-- DOC DISCIPLINE | Soft ceiling: 500 lines. One topic per file; structure under ## headings.
     The DOC line above feeds `phaneslight doc-index`, keep it accurate; it is this file's line in _index.md.
     If this file exceeds the ceiling: split it into a same-named folder of focused topic files;
     carry both header lines into every part; update every inbound reference in the same change set;
     finish by running `phaneslight doc-index`.
     Consumers: NEVER bulk-read documentation folders, read _index.md first, load only what you need.
     Audit: `phaneslight doc-check`. -->

# SS00003 — PhanesLight bootstrap pre-merge hardening

**Date:** 2026-09-19 · **Scope:** final pre-merge hardening of `a59917a4` + `1a040c28` ·
**Previous:** SS00002
**Authorisation:** operator-granted for this bootstrap only — safe shell, Git inspection,
documentation checks and non-DB validation; invocation of `afldb-closure`; one further follow-up
commit. No push, no merge, no deploy, no SSH, no database. `a59917a4` and `1a040c28` were not
amended. **`CLAUDE.md` §1–§14 resume in full after this pass.**

## 1. Restart confirmed the generated infrastructure is live

SS00002 §10 recorded that nothing generated by the bootstrap was live until Claude Code restarted.
After the restart:

- The `SessionStart` hooks ran and reported **success**, with no error and no blocking.
- `hook-ledger-status` produced **no output**, which is its contract: it prints exactly one line
  only when a prior run died mid-flight. `.phaneslight/run-progress` ends `CLOSED, run complete`, so
  silence is the correct and expected result — SS00002's "has never yet run" carry-forward is now
  closed.
- All **five** generated agents are discoverable and dispatchable: `afldb-orchestrator`,
  `afldb-reviewer`, `afldb-worker`, `afldb-mechanic`, `afldb-closure`.
- Hook wiring lives in the `phaneslight` Claude Code **plugin**
  (`hooks/hooks.json`: `PreToolUse:Write` → `hook-stamp-guard`, `PostToolUse:Write|Edit` →
  `hook-size-check`, `SessionStart` → `hook-ledger-status` + the install notice), activated
  per-machine through the gitignored `.claude/settings.local.json`. The hook **scripts** are
  tracked; the **wiring** is not. See F-C02 below.

## 2. Issue register corrected against repository evidence

SS00002's F-002 and F-003 were **wrong in direction**, and are superseded. Re-derived from the
detailed `**Status:**` lines in `issues.md` (not from the summary table) and cross-checked against
Git:

| Issue | Detailed status | Evidence |
|---|---|---|
| AFLDB-ISSUE-222 | **RESOLVED 2026-09-19** | `fe9fa03c` "docs(issue-222): close trusted DraftGuru linkage rollout" |
| AFLDB-ISSUE-223 | **Resolved 2026-09-19** | `a76a2088` "fix(tests): AFLDB-ISSUE-223 restore draft vocabulary parity" |
| AFLDB-ISSUE-220 / 224 / 225 / 226 | Open | detailed entries |

Both resolved issues had already been removed from `IssuesIndex.md` by their own closing commits —
that is §5's resolved-issue convention working correctly, **not** a missing index entry. The real
defect was the opposite one: `issues.md`'s Open Issues **table** still carried stale `Open` rows for
both, and its count read 6.

Applied:

- `issues.md` — removed the stale table rows for AFLDB-ISSUE-222 and AFLDB-ISSUE-223; count
  **6 → 4**. No detailed entry was altered, and no entry was fabricated.
- `IssuesIndex.md` — removed the "Index incomplete" warning block added by `1a040c28`, which
  asserted a missing-entry problem that does not exist; count line restored to `**Open issues:** 4`.
- **AFLDB-ISSUE-226 preserved** unchanged. Its allocation was re-verified: no `## AFLDB-ISSUE-227`
  heading exists, so `227` remains genuinely unallocated and `226` was the correct next number.

Both files now agree: exactly **4 open issues** — 220, 224, 225, 226.

## 3. `CLAUDE.md` capacity restored

`register-check`: **34,714 → 32,974 chars** (−1,740). Headroom to the 35,000 soft breach is
**2,026**; to the 40,000 crop trigger, **7,026**.

What changed — **§15 only**:

- `CLAUDE.md` §1–§14 and the Purpose/lifecycle header were verified **byte-identical to
  `origin/main`** (LF-normalised comparison; the working tree is CRLF and git blobs are LF, so a
  naive diff misreports the whole file).
- The `pinned:phaneslight` and `pinned:project` blocks were **not touched**. All four owner-authored
  DEVIATION entries are intact and unweakened.
- The **Installed Capability Register** was **not touched** — it is GENERATED and hand-editing it is
  forbidden. It remains byte-identical.

Moved out to `documentation/architecture/2026-09-19_initial/agent-operating-model.md` (new, 182
lines), which explicitly subordinates itself to §15 and to §1–§14 and creates no obligation:
engagement rationale; the mechanics of why the write column is prose-enforced; the nesting-bound
proof; why domain expertise is not baked into agent files; why the mechanic escalates one grade
lower; the T1 boundary cases; the three Playwright capture configs; the declined-capability
rationale; the API-baseline explanation; and the **full script command table**.

Condensed in place, with no binding rule dropped: the precedence blockquote, the engagement
bullets, the post-lineup paragraph, the escalation bullets, the tier-triage cells, the
visual-evidence paragraphs, the documentation-navigation rule and the workflows paragraph.

Every binding rule named in §15 survives: engagement threshold, the five-row lineup table,
no-forking, no agent invoking the orchestrator, the harness-versus-prose enforcement split, the
CRIT/HIGH/MED/LOW/INFO ladder and who escalates what to whom, plan review at launch, the T1/T2/T3
table, the promotion rule, universal disclosure, the Visual Evidence Mandate including the
prose-approval prohibition, the documentation navigation rule, the `node .phaneslight/scripts/cli.js`
invocation rule, `new-file` as the only sanctioned creation, the §9 not-authorisation caveat, and
workflows-never-redefine-routing.

## 4. Independent review by the real `afldb-closure` agent

The generated `afldb-closure` agent was dispatched **read-only** (no writes, no DB, no SSH, no
push/merge/commit, no build/test suite) over `origin/main..HEAD` plus the uncommitted working tree,
across twelve areas. Verdict: **READY TO MERGE**, **no CRIT, HIGH or MED finding**.

| ID | Grade | Disposition |
|---|---|---|
| F-C01 | INFO | `documentation/archive/projects/phaneslight-bootstrap.md` digest has one 1,102-char bullet — within the ≤15-line contract but not pointer-shaped. **Accepted, not changed**: rewriting an archived digest is outside this pass. |
| F-C02 | INFO | Hook **wiring** is plugin-registered and machine-local via the gitignored `.claude/settings.local.json`; only the hook **scripts** are tracked. **Re-verified** (`git check-ignore -v .claude/settings.local.json` → `.gitignore:27`). Plugin-architecture-inherent, not a repository defect. **Recorded as a standing caveat**: a fresh clone gets the scripts, not armed hooks. |
| F-C03 | INFO | `hook-size-check.ps1` invokes `doc-index` (a write) on any edit under `documentation/`. **Re-verified** by reading `hook-size-check.ps1:52-58`. By design; noted so future read-only dispatches expect it. |
| F-C04 | LOW | Compaction dropped the named-actor sentence "An undisclosed edit is reported as drift by `afldb-closure`". **Re-verified** against `1a040c28:CLAUDE.md:670` and **FIXED**: the drift rule now reads "an **undisclosed edit is drift, not an oversight**: `afldb-closure` reports it as such" in §15's disclosure paragraph. |
| F-C05 | INFO | Size headroom 2,026 chars — tighter than ideal. **Accepted**: reducing further would require editing the GENERATED capability register or the pinned block, neither of which this pass may touch. |

**One protocol deviation by the closure agent, recorded rather than smoothed over.** Its brief said
it must not create any file. It nonetheless wrote six scratch comparison copies (CLAUDE.md at three
revisions, LF-normalised variants, and a changed-file list). It intended them for `D:\tmp`, but the
Windows path was mangled by its shell into literal filenames beginning `D:` **in the repository
root**, where they showed up as untracked files. They were inspected, confirmed to be pure derived
copies with no unique content, and deleted by the primary session before the commit. No tracked file
was touched and the review's findings are unaffected — but this is exactly the class of undisclosed
side effect §15's disclosure rule exists to catch, and it was caught by `git status`, not by the
agent's own report.

Closure explicitly declined to assert two duties it was not authorised to perform: `npm run build` /
`npm test`, and `loc-check`. Both were closed afterwards by the primary session:

- `loc-check` run: **343 files over the 500-line soft ceiling, exit 0, advisory** — matching
  SS00002's carry-over claim, now independently re-derived. The one new file,
  `agent-operating-model.md`, is 182 lines.
- Build and test suites were **not** run and are **not** claimed: the operator authorised
  non-DB validation only, and the change set touches **zero** files under `src/` with **zero** API
  surface changes (`api-diff origin/main` → 0 added / 0 removed / 0 changed across 17 slices), so
  there is no application behaviour for a build or suite to regress. `npx tsc --noEmit` was clean in
  the closure dispatch.

## 5. Validation re-run after corrections

| Check | Result |
|---|---|
| `cli.js update-preflight` | hooks 0 delta, all 3 scripts present, no stale project entry; register OK; manifest 34 checked, **0 drifted, 0 missing**; modules 16; customizations 0 |
| `cli.js doc-index` | regenerated 25 index files |
| `cli.js doc-check` | **OK** |
| `cli.js register-check` | `CLAUDE.md` **32,974 [OK]**, pinned block 4,534; `CLAUDE.local.md` 3,051 [OK] |
| `cli.js loc-check` | 343 over soft ceiling, **exit 0**, advisory |
| `cli.js api-diff origin/main` | **0 added, 0 removed, 0 changed** across 17 slices |
| `cli.js repo-manifest` | `staleCount: 0` |
| `git diff --check` | clean (only the repository-normal LF→CRLF advisory) |
| `git status --short` | only the disclosed files |

`update-preflight`'s census sensor reports **7 added / 4 removed** capabilities. This is
session-environment churn (the MCP/skill roster differs between the bootstrap session and this one),
not repository drift; `.phaneslight/config.json`'s `capabilities.selection[]` is unchanged and no
grant in the Installed Capability Register moved.

## 6. Files touched by this pass

- `CLAUDE.md` — §15 compacted; F-C04 wording restored. §1–§14 and both pinned blocks untouched.
- `issues.md` — Open Issues table: two stale resolved rows removed, count 6 → 4.
- `IssuesIndex.md` — inaccurate warning block removed, count line restored.
- `documentation/architecture/2026-09-19_initial/agent-operating-model.md` — **new**.
- `documentation/architecture/2026-09-19_initial/_index.md` — regenerated by `doc-index`.
- `documentation/session-summaries/SS00002_bootstrap-closure-review_2026-09-19.md` — F-002, F-003
  and F-007 marked superseded, with pointers here. No narrative rewritten.
- `documentation/session-summaries/SS00003_bootstrap-premerge-hardening_2026-09-19.md` — this file.
- `documentation/session-summaries/_index.md` — regenerated by `doc-index`.

`CHANGELOG.md` was deliberately **not** updated: the change set alters no application, search, data,
admin or deployment behaviour, which is §5's bar for an entry.

## 7. Fan-out ledger

| Phase | Agents spawned | Peak in flight |
|---|---|---|
| Independent review | 1 (`afldb-closure`, read-only) | **1** |

No other agent was spawned. No agent was forked. `afldb-closure` spawned nothing, as its grant
requires.

## 8. Open TODOs carried forward

1. **F-C02 (INFO):** hook enforcement depends on the `phaneslight` plugin being installed and
   enabled in each operator's own Claude Code installation. A clone alone does not arm the hooks.
2. **F-C01 (INFO):** the bootstrap archive digest's single 1,102-char bullet could be reduced to
   pointer form at a future documentation pass.
3. **F-C05 / F-007 (INFO):** `CLAUDE.md` headroom is 2,026 chars. The next material §15 or pinned
   addition should be paired with a compaction, and the first candidate is the GENERATED capability
   register — which only a PhanesLight run may rewrite.
4. **F-006 (LOW, from SS00002):** `.phaneslight/manifest.json` covers 34 scripts/hooks/templates but
   not the generated agents, workflows or doc scaffolds. Unchanged by this pass.
5. **M-4 (from SS00002):** repository write rights remain prose-enforced, caught by disclosure plus
   closure reconciliation rather than by the harness.
